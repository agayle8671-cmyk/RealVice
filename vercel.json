{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/leonida-vice run build:vercel && mkdir -p dist && cp -r artifacts/leonida-vice/dist/* dist/ && ls -la dist",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
