{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/leonida-vice run build:vercel && ls -la && ls -la dist",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
