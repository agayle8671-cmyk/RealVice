{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/leonida-vice run build:vercel && pwd && ls -la && test -d dist && echo 'dist exists!' || echo 'dist missing!'",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
